import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Guidelines/Design Principles" />

<div className="max-w-4xl p-10 font-sans">
  <div className="mb-12">
    <h1 className="text-5xl font-bold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
      Design Philosophy
    </h1>
    <p className="text-xl text-slate-500">
      The guiding principles behind Billflow's neo-brutalist design system.
    </p>
  </div>

  <section className="mb-12">
    <h2 className="text-2xl font-semibold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
      Neo-Brutalist Design
    </h2>
    <p className="text-slate-600 mb-6 leading-relaxed">
      Neo-brutalism is a modern design movement characterized by bold colors, thick borders,
      hard shadows, and confident typography. Unlike minimal designs that fade into the background,
      neo-brutalist interfaces make a statement — perfect for a professional invoicing application
      that needs to convey trust, clarity, and confidence.
    </p>

    <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
      <div className="p-4 rounded-lg border-2 border-black" style={{ boxShadow: '4px 4px 0 0 #000' }}>
        <h3 className="font-semibold mb-2">Bold Borders</h3>
        <p className="text-sm text-slate-500">
          2-4px solid black borders define every interactive element, creating clear visual hierarchy
          and making UI components feel tangible.
        </p>
      </div>
      <div className="p-4 rounded-lg border-2 border-black" style={{ boxShadow: '4px 4px 0 0 #000' }}>
        <h3 className="font-semibold mb-2">Hard Shadows</h3>
        <p className="text-sm text-slate-500">
          No blur, just pure offset. This creates a distinctive graphic quality that sets Billflow
          apart from conventional SaaS applications.
        </p>
      </div>
      <div className="p-4 rounded-lg border-2 border-black" style={{ boxShadow: '4px 4px 0 0 #000' }}>
        <h3 className="font-semibold mb-2">Confident Typography</h3>
        <p className="text-sm text-slate-500">
          Space Grotesk for headings brings a tech-forward feel, while Noto Sans TC ensures
          excellent readability in both English and Chinese.
        </p>
      </div>
      <div className="p-4 rounded-lg border-2 border-black" style={{ boxShadow: '4px 4px 0 0 #000' }}>
        <h3 className="font-semibold mb-2">High Contrast</h3>
        <p className="text-sm text-slate-500">
          Every color combination meets WCAG AA standards. Financial data must be crystal clear
          with no ambiguity.
        </p>
      </div>
    </div>
  </section>

  <section className="mb-12">
    <h2 className="text-2xl font-semibold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
      Core Principles
    </h2>

    <div className="space-y-6">
      <div className="flex gap-4 items-start p-4 bg-slate-50 rounded-lg border-2 border-black">
        <div className="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center text-white font-bold shrink-0">1</div>
        <div>
          <h3 className="font-semibold">Clarity Over Decoration</h3>
          <p className="text-sm text-slate-500">
            Every visual element serves a purpose. Financial data demands clarity — users need to
            understand invoice amounts, statuses, and due dates at a glance.
          </p>
        </div>
      </div>

      <div className="flex gap-4 items-start p-4 bg-slate-50 rounded-lg border-2 border-black">
        <div className="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center text-white font-bold shrink-0">2</div>
        <div>
          <h3 className="font-semibold">Consistency Builds Trust</h3>
          <p className="text-sm text-slate-500">
            Consistent patterns help users feel confident. Every button, card, and form follows
            the same visual language, reducing cognitive load.
          </p>
        </div>
      </div>

      <div className="flex gap-4 items-start p-4 bg-slate-50 rounded-lg border-2 border-black">
        <div className="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center text-white font-bold shrink-0">3</div>
        <div>
          <h3 className="font-semibold">Accessibility First</h3>
          <p className="text-sm text-slate-500">
            WCAG 2.2 Level AA compliance is non-negotiable. Keyboard navigation, screen reader
            support, and color contrast are built into every component.
          </p>
        </div>
      </div>

      <div className="flex gap-4 items-start p-4 bg-slate-50 rounded-lg border-2 border-black">
        <div className="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center text-white font-bold shrink-0">4</div>
        <div>
          <h3 className="font-semibold">Delightful Interactions</h3>
          <p className="text-sm text-slate-500">
            Micro-interactions add personality without slowing users down. Buttons lift on hover,
            cards have tactile press states, and forms give immediate feedback.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section className="mb-12">
    <h2 className="text-2xl font-semibold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
      Component Guidelines
    </h2>

    <div className="rounded-lg border-2 border-black overflow-hidden">
      <table className="w-full">
        <thead className="bg-slate-50">
          <tr>
            <th className="text-left p-3 border-b-2 border-black font-semibold">Principle</th>
            <th className="text-left p-3 border-b-2 border-black font-semibold">Implementation</th>
          </tr>
        </thead>
        <tbody>
          <tr className="border-b border-slate-200">
            <td className="p-3 font-semibold">Use design system components</td>
            <td className="p-3 text-slate-600">Always use components from this library rather than creating custom implementations</td>
          </tr>
          <tr className="border-b border-slate-200">
            <td className="p-3 font-semibold">Props over classes</td>
            <td className="p-3 text-slate-600">Use component props like <code className="bg-slate-100 px-1 rounded">variant</code> and <code className="bg-slate-100 px-1 rounded">size</code> instead of custom CSS</td>
          </tr>
          <tr className="border-b border-slate-200">
            <td className="p-3 font-semibold">Composition pattern</td>
            <td className="p-3 text-slate-600">Components are designed to be composed. Combine Card, Input, and Button to build forms</td>
          </tr>
          <tr className="border-b border-slate-200">
            <td className="p-3 font-semibold">Mobile-first responsive</td>
            <td className="p-3 text-slate-600">Start with mobile layout, then enhance for larger screens using breakpoints</td>
          </tr>
          <tr>
            <td className="p-3 font-semibold">Semantic status colors</td>
            <td className="p-3 text-slate-600">Green for success/paid, yellow for pending, red for error/overdue, blue for info/sent</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <section className="mb-12">
    <h2 className="text-2xl font-semibold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
      Animation Principles
    </h2>
    <p className="text-slate-600 mb-6">
      Motion.dev (Framer Motion) powers all animations with spring physics and easing presets.
    </p>

    <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
      <div className="p-4 rounded-lg border-2 border-black">
        <h3 className="font-semibold mb-3">Duration Guidelines</h3>
        <div className="space-y-2 text-sm">
          <div className="flex justify-between">
            <span className="text-slate-500">Instant (micro)</span>
            <code className="bg-slate-100 px-2 py-0.5 rounded">100ms</code>
          </div>
          <div className="flex justify-between">
            <span className="text-slate-500">Fast (button states)</span>
            <code className="bg-slate-100 px-2 py-0.5 rounded">150ms</code>
          </div>
          <div className="flex justify-between">
            <span className="text-slate-500">Normal (transitions)</span>
            <code className="bg-slate-100 px-2 py-0.5 rounded">250ms</code>
          </div>
          <div className="flex justify-between">
            <span className="text-slate-500">Slow (page transitions)</span>
            <code className="bg-slate-100 px-2 py-0.5 rounded">400ms</code>
          </div>
        </div>
      </div>

      <div className="p-4 rounded-lg border-2 border-black">
        <h3 className="font-semibold mb-3">Easing Presets</h3>
        <div className="space-y-2 text-sm">
          <div className="flex justify-between">
            <span className="text-slate-500">Out (default)</span>
            <code className="bg-slate-100 px-2 py-0.5 rounded">[0.22, 1, 0.36, 1]</code>
          </div>
          <div className="flex justify-between">
            <span className="text-slate-500">In-Out</span>
            <code className="bg-slate-100 px-2 py-0.5 rounded">[0.65, 0, 0.35, 1]</code>
          </div>
          <div className="flex justify-between">
            <span className="text-slate-500">Bounce</span>
            <code className="bg-slate-100 px-2 py-0.5 rounded">[0.68, -0.55, 0.265, 1.55]</code>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section>
    <h2 className="text-2xl font-semibold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
      Taiwan-Specific Considerations
    </h2>
    <p className="text-slate-600 mb-6">
      Billflow is designed for the Taiwan market with specific localization requirements.
    </p>

    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
      <div className="p-4 rounded-lg border-2 border-black">
        <h3 className="font-semibold mb-2">Currency</h3>
        <div className="text-2xl font-mono mb-2">NT$1,234.56</div>
        <p className="text-sm text-slate-500">New Taiwan Dollar with comma separators</p>
      </div>
      <div className="p-4 rounded-lg border-2 border-black">
        <h3 className="font-semibold mb-2">Tax ID</h3>
        <div className="text-2xl font-mono mb-2">12345678</div>
        <p className="text-sm text-slate-500">8-digit Unified Business Number (統一編號)</p>
      </div>
      <div className="p-4 rounded-lg border-2 border-black">
        <h3 className="font-semibold mb-2">Date Format</h3>
        <div className="text-2xl font-mono mb-2">2024/01/15</div>
        <p className="text-sm text-slate-500">YYYY/MM/DD format standard</p>
      </div>
    </div>
  </section>
</div>
